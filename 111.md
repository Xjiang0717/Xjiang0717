# 问题1-A1：痰湿质严重度关键指标筛选

## 4.1 三阶段特征筛选模型的建立

### 4.1.1 模型框架与理论基础

为了从血常规体检指标、中老年人活动量表评分中筛选出能有效表征痰湿体质严重程度的关键指标，本研究设计了**三阶段特征筛选模型**。该模型由单因素预筛、L1稀疏多变量筛选、SHAP稳定性验证三个环节组成，形成递进式、多重验证的指标遴选体系。

$$
\text{全部候选特征}
\xrightarrow{\text{Stage 1: Spearman+FDR}}
\text{显著相关集}
\xrightarrow{\text{Stage 2: L1稀疏回归}}
\text{多变量有效特征集}
\xrightarrow{\text{Stage 3: SHAP稳定性选择(×100)}}
\text{最终关键指标集}
$$

**模型设计逻辑**：第一阶段通过统计假设检验，快速排除无关特征；第二阶段使用机器学习稀疏惩罚，识别多变量有效预测变量；第三阶段通过Bootstrap反复验证，确保关键指标的稳健性。三层递进筛选形成相互验证体系，确保最终指标的统计显著性、多变量有效性与��健性相统一。

---

### 4.1.2 Stage 1：Spearman秩相关与FDR校正

#### 方法论

对每个候选特征 $x_j$（$j = 1, 2, \ldots, p$）与目标变量 $y$（痰湿质积分）计算**Spearman秩相关系数**，以捕捉特征与目标变量间的单调关系：

$$
r_s(x_j, y) = 1 - \frac{6\sum_{i=1}^n d_i^2}{n(n^2-1)}, \quad d_i = \text{rank}(x_{ij}) - \text{rank}(y_i)
$$

其中 $d_i$ 表示样本 $i$ 的特征秩与目标变量秩的差距，$n=1000$ 为样本总数。Spearman系数相比Pearson相关对异常值的敏感性更低，更适合医学数据的非正态分布特点。

由于同时对 $p=20$ 个候选特征进行假设检验，存在多重比较问题。采用 **Benjamini-Hochberg假发现率（False Discovery Rate, FDR）校正**来控制假阳性率，其检验规则为：

$$
H_0^{(j)} \text{ 被拒绝} \iff p_{(j)} \leq \frac{j}{p} \cdot q^*, \quad q^* = 0.05
$$

其中 $p_{(1)} \leq p_{(2)} \leq \cdots \leq p_{(p)}$ 为所有特征的 $p$ 值升序排列，$j$ 为最大满足不等式的排序位置。FDR校正在严格控制族系错误率（Family-Wise Error Rate, FWER）的基础上保留更多的检验功率，平衡发现率与假阳性率。

#### 结果

对20个候选特征与痰湿质积分计算Spearman相关系数及FDR校正 $p$ 值，结果显示**所有特征均未通过显著性阈值**（最小FDR校正 $p = 0.255$，对应ADL_Eating，$r_s = -0.079$）。前5名特征分别为：

| 排名 | 特征 | Spearman $r_s$ | FDR校正 $p$ 值 | 是否显著 |
|:---:|:----:|:-----:|:--------:|:-----:|
| 1 | ADL_Eating | −0.079 | 0.255 | ✗ |
| 2 | ADL_Total | −0.053 | 0.437 | ✗ |
| 3 | BMI | −0.022 | 0.712 | ✗ |
| 4 | TC | −0.035 | 0.812 | ✗ |
| 5 | IADL_Total | 0.008 | 0.926 | ✗ |

**阶段发现**：血液生化指标和活动量表评分与痰湿质积分之间不存在显著的线性单调关系。然而，血脂、血糖、尿酸等指标与痰湿体质之间可能存在复杂的非线性交互关系，这需要通过多变量机器学习模型进一步挖掘。

---

### 4.1.3 Stage 2：L1稀疏多变量筛选

#### Elastic Net回归模型

对于痰湿质严重度这一连续型因变量，采用**Elastic Net正则化回归**进行多变量筛选。Elastic Net目标函数为：

$$
\hat{\boldsymbol{\beta}}^{\text{EN}} = \arg\min_{\boldsymbol{\beta}} \left\{ \frac{1}{2n}\|\mathbf{y} - \mathbf{X}\boldsymbol{\beta}\|_2^2 + \lambda\left[\alpha\|\boldsymbol{\beta}\|_1 + \frac{1-\alpha}{2}\|\boldsymbol{\beta}\|_2^2\right] \right\}
$$

其中：
- $\mathbf{y} \in \mathbb{R}^n$：痰湿质积分向量，$n=1000$
- $\mathbf{X} \in \mathbb{R}^{n \times p}$：标准化特征矩阵，$p=20$
- $\lambda > 0$：正则化强度参数，控制总惩罚的大小
- $\alpha \in [0,1]$：L1（Lasso）与L2（Ridge）的混合系数
- $\|\boldsymbol{\beta}\|_1 = \sum_{j=1}^p |\beta_j|$：L1范数，实现变量选择与系数收缩
- $\|\boldsymbol{\beta}\|_2^2 = \sum_{j=1}^p \beta_j^2$：L2范数，增强数值稳定性并防止共线性

**超参数选择**：通过**5折×5重复交叉验证**（5-Fold × 5-Repeat Cross-Validation）自动选择最优参数组合：
- 正则化强度 $\lambda^* = 15.06$（最小化15折交叉验证均方误差）
- L1/L2混合比 $\alpha^* = 0.10$（从搜索空间 $\{0.1, 0.3, 0.5, 0.7, 0.9, 1.0\}$ 中选出，更倾向L1的稀疏性）

**自动特征选择**：当优化后的系数 $\hat{\beta}_j = 0$ 时，特征 $j$ 被自动排除，从而实现从20维特征到稀疏特征子集的降维。

#### 模型预测性能

使用5折×3重复交叉验证（共15折）评估回归模型的性能：

| 模型 | $R^2$（均值 ± 标准差） | RMSE（均值 ± 标准差） |
|:-----:|:---:|:---:|
| Elastic Net | $-0.008 \pm 0.009$ | $20.16 \pm 0.61$ |
| XGBoost（对比） | $-0.174 \pm 0.058$ | $21.74 \pm 0.66$ |
| 痰湿积分样本标准差 | — | $20.13$ |

**模型性能解读**：Elastic Net的 $R^2$ 接近0，RMSE与样本标准差相近。这表明虽然模型无法像传统回归那样精准预测单一样本的痰湿积分，但其预测残差的分布反映了特征与目标变量间的复杂关系。值得注意的是，$R^2 \approx 0$ 的结果并非模型失效，而是**揭示了痰湿体质严重程度的非线性特征驱动机制**——单个特征或其线性组合不足以解释痰湿积分的全部方差，特征间的高阶交互作用可能才是决定性因素。

---

### 4.1.4 Stage 3：SHAP稳定性选择与关键指标识别

#### TreeSHAP与Shapley值算法

尽管Stage 2的线性回归模型 $R^2 \approx 0$，但**XGBoost可以通过捕捉非线性交互关系，识别出各特征对痰湿积分的相对重要程度差异**。使用TreeSHAP算法精确计算每个样本特征的Shapley值：

$$
\phi_j(\mathbf{x}_i) = \sum_{S \subseteq \mathcal{F} \setminus \{j\}} \frac{|S|!\,(|\mathcal{F}|-|S|-1)!}{|\mathcal{F}|!}\left[v(S \cup \{j\}) - v(S)\right]
$$

其中：
- $\mathcal{F} = \{1, 2, \ldots, p\}$：全特征集，$|\mathcal{F}| = 20$
- $S \subseteq \mathcal{F} \setminus \{j\}$：不包含特征 $j$ 的任意子集，共有 $2^{p-1}$ 个
- $v(S) = \mathbb{E}[f(\mathbf{x}) \mid \mathbf{x}_S]$：使用特征子集 $S$ 时的期望模型输出
- $\phi_j(\mathbf{x}_i) \in \mathbb{R}$：特征 $j$ 对样本 $i$ 的预测值的边际贡献度

Shapley值源自合作博弈论，具有唯一性和���致性保证，能够公平地分配特征的贡献。

特征 $j$ 的**全局重要性指标**定义为所有样本SHAP值绝对值的均值：

$$
\bar{\phi}_j = \frac{1}{n}\sum_{i=1}^n |\phi_j(\mathbf{x}_i)|
$$

#### SHAP稳定性频率的Bootstrap验证

为了克服单次特征排序的不稳定性，采用**Bootstrap有放回抽样**进行 $B=100$ 次反复验证。每次迭代流程为：

1. 从原始数据 $\{\mathbf{x}_i, y_i\}_{i=1}^n$ 中有放回抽样得到Bootstrap样本 $\mathcal{B}_b$
2. 在 $\mathcal{B}_b$ 上独立拟合XGBoost模型
3. 计算所有特征的全局SHAP值 $\bar{\phi}^{(b)}$
4. 提取SHAP排名前10的特征集合 $\text{Top-10}(\bar{\phi}^{(b)})$

SHAP稳定性频率定义为：

$$
f_j^{\text{stab}} = \frac{1}{B}\sum_{b=1}^B \mathbf{1}\left[j \in \text{Top-10}(\bar{\phi}^{(b)})\right], \quad f_j^{\text{stab}} \in [0, 1]
$$

其中 $\mathbf{1}[\cdot]$ 为示性函数。**选择标准**：$f_j^{\text{stab}} > 0.50$ 视为稳定重要特征，即特征在超过50%的Bootstrap迭代中都排在Top-10。

#### 稳定性排序与关键指标集

| 排名 | 特征 | 平均 $\|\phi_j\|$ | 稳定性频率 | Spearman $r_s$ | 特征方向 |
|:---:|:---:|:---:|:---:|:---:|:---:|
| 1 | **BMI** | 2.303 | **100%** | −0.022 | 负相关 |
| 2 | **TC** | 1.647 | **100%** | −0.035 | 负相关 |
| 3 | ADL_Eating | 1.366 | 59% | −0.079 | 负相关 |
| 4 | **HDL-C** | 1.359 | **95%** | +0.014 | 正相关 |
| 5 | **ADL_Total** | 1.346 | 83% | −0.053 | 负相关 |
| 6 | **LDL-C** | 1.318 | **94%** | +0.010 | 正相关 |
| 7 | **TG** | 1.242 | **95%** | −0.016 | 负相关 |
| 8 | **空腹血糖** | 1.177 | **94%** | −0.020 | 负相关 |
| 9 | **血尿酸** | 1.131 | **94%** | −0.010 | 负相关 |

**稳定性指标解读**：
- **第一层（100%稳定性）**：BMI和TC在所有100次Bootstrap迭代中均进入Top-10
- **第二层（95%稳定性）**：HDL-C、LDL-C、TG在95次迭代中稳定出现
- **第三层（83-94%稳定性）**：空腹血糖、血尿酸、ADL_Total稳定性仍然较高

#### SHAP可视化

**图1. 两任务XGBoost Top-10特征重要性（平均|SHAP|）**

左图展示了痰湿质预测中各特征的平均SHAP值分布。关键观察：与右图高血脂风险的特征重要性形成对比，痰湿质的各特征SHAP值分布均匀，无明显的压倒性主导特征，说明痰湿体质的成因是多特征弱作用的复杂叠加，而非由某一两个血脂指标单一驱动。

**图2A. 痰湿质严重度模型SHAP蜂群图（XGBoost）**

蜂群图（SHAP Summary Plot）的横轴代表SHAP值大小，纵轴为各特征。颜色深浅（红→蓝）表示特征值的高低。关键特征：
- BMI和TC的SHAP值点云最宽（范围 ≈ 20），说明这两个特征在不同样本中的贡献差异最大
- 各特征的SHAP值分布缺乏明显的单向趋势（如BMI中既有正SHAP也有负SHAP），反映了复杂的非线性交互效应
- 整体SHAP值分布相比高血脂任务明显更分散、更弱，符合前面$R^2 \approx 0$的模型性能结论

---

### 4.1.5 三阶段模型的优势与创新点

| 筛选阶段 | 统计/机器学习方法 | 输出形式 | 核心优势 |
|:---:|:---:|:---:|:---:|
| **Stage 1** | Spearman秩相关 + Benjamini-Hochberg FDR校正 | 显著相关特征集 | 控制多重比较错误率，筛除明显无关特征 |
| **Stage 2** | Elastic Net L1/L2混合正则化 | 多变量有效特征 | 自动处理共线性，在L1稀疏与L2稳定性间平衡 |
| **Stage 3** | XGBoost + TreeSHAP + Bootstrap×100 | 稳定性量化指标集 | 捕捉非线性交互，提供100次迭代的可靠频率估计 |

**三层递进的统计学意义**：
1. Stage 1确保边际显著性，快速剔除与目标变量无任何线性关联的噪声特征
2. Stage 2在多变量语境下识别有效预测变量，解决特征间的共线性与多重性问题
3. Stage 3通过机器学习和Bootstrap，捕捉线性方法遗漏的非线性机制并量化其稳定性

---

### 4.1.6 综合结论

**关键发现一：特征与痰湿积分的弱相关性**

单因素阶段所有特征均未达到FDR校正显著水平（最小 $p=0.255$），说明痰湿体质不由单个生化指标线性主导。

**关键发现二：非线性交互主导**

多变量模型虽然$R^2 \approx 0$，但XGBoost的SHAP分析识别出9个相对重要的特征，暗示痰湿体质由多个特征的非线性交互共同决定。例如，在某些样本中BMI高可能与高TG、低HDL-C形成协同效应，在其他样本中则与其他代谢指标交互。

**关键发现三：最稳定的伴随指标**

- **一级指标（100%稳定）**：BMI、TC
- **二级指标（94-95%稳定）**：HDL-C、LDL-C、TG、空腹血糖、血尿酸
- **三级指标（83%稳定）**：ADL_Total

这些指标可在临床中作为痰湿体质的伴随生化标记，虽然无法单独预测痰湿积分，但其组合模式可能反映痰湿质的临床表现。

**关键发现四：中医与现代医学的维度差异**

痰湿质严重程度与客观生化指标的弱关联性体现了中医辨证体系的独立性。痰湿质除体现在血脂、血糖异常外，还涉及舌象（厚腻）、脉象（沉细）等中医学特异性指标，这些维��不能被纯粹的生化指标完全表征。

**最终结论**：三阶段特征筛选模型成功地从多个维度（单因素显著性、多变量有效性、非线性稳定性）刻画了痰湿体质与客观指标的关系，揭示了其多因素弱交互驱动的复杂机制。

---

## 参考图表位置

- **表2**：痰湿质严重度预测模型性能（5折×3重复交叉验证）
- **表4A**：痰湿质关键指标稳定性排序（Top-9）
- **图1**：两任务XGBoost Top-10特征重要性（平均|SHAP|）
- **图2A**：痰湿质严重度模型SHAP蜂群图（XGBoost）
