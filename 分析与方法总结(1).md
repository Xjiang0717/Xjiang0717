# 2026全国大学生数学建模竞赛 C题 — 分析与方法总结

> 工作目录：`C:\Users\GuoJiarong\Desktop\C题`  
> 最后更新：2026-09-11（整合三版方案分析 + 全部数值验证结果）

---

## 一、核心场景与数据全貌

微网调度：利用光伏（PV）发电、储能设备（Battery）和外网购电，以最低费用满足小区负载。

### 数据结构（已实测验证）

| 附件 | 内容 | 规模 | 实测关键发现 |
|---|---|---|---|
| 附件1 | 电价、负载、PV预测 | 144行×4列，10分钟粒度 | **已验证 = 附件2/4全年逐时段均值**（RMSD：电价2.9e-05，负载3.2e-05，PV5.5e-03） |
| 附件2 | 全年实际负载 + 实际PV | 365行×144列，2张表 | 负载 14.1% 日际波动（持久法），PV 7.7% |
| 附件3 | 全年每天0/6/12/18时PV预报 | 1461行×25列，**整点小时粒度** | 小时值→10分钟必须梯形积分，否则 ±3000 kWh 伪误差 |
| 附件4 | 全年实际电价 | 365行×144列 | 日内 max/min 比值均值 5.74，全部365天超过套利阈值 1.235 |

### 储能参数（附录1）

| 参数 | 值 |
|---|---|
| 可用容量 | 1200–10800 kWh（**9600 kWh 可用**） |
| 单槽充放电上限 | 5000/6 = **833.33 kWh** |
| 充放电效率 | **η = 90%**（双向各损10%） |
| 初始SOC（2025-1-1 0:00） | **6000 kWh** |

---

## 二、最关键的数据发现（均已独立复现）

### 发现1：附件1 = 全年均值日（逐时段完全吻合）

附件1不是"某一天"，而是附件2/4按时段取全年平均的结果。**这是串联四问的基础**：Q1解的是基准期望日的最优策略；Q4是在这条均值基线上叠加实时偏差的问题。

### 发现2：Q1 光伏溢出精确数据

- 溢出时段：09:30–14:30（30个时槽）
- 溢出总能量：**6248.0 kWh**；最大溢出功率：**2117.5 kW**
- 完全吸收光伏所需的09:40前SOC上界：$10800 - 0.9 \times 6248 = 5177$ kWh
- **实际最优解不是"接住光伏"而是"趁午间低价大量买电充储"**：Q1最优解在09:30–14:30内净充入9961 kWh（远超6248），因此约束的本质是"为午间廉价电+光伏联合充电留出空间"，而非单纯保留光伏头寸。

### 发现3：Q1 LP松弛自然满足充放电互斥

实测结果：`max_t min(c_t, d_t) = 0.000000`，零时槽同时充放。  
**正确表述**：加二元变量 $u^c_t + u^d_t \le 1$ 是为物理可执行性背书，不是"防止LP套利"。LP松弛解本身就是可执行的——这是可以报告给评委的有力验证结论。

### 发现4：价格–净负荷耦合是本题最有价值的单点发现

- 原始相关（电价 vs 净负荷绝对水平）：+0.26（弱）
- **扣除均值日基线后**（电价偏差 vs 净负荷偏差）：**+0.9165**
- 1月拟合的线性模型 $\hat p_{d,t} = \bar p_t + \beta_t (\widehat{\text{net}}_{d,t} - \overline{\text{net}_t})$，在2月–12月样本外 **R² = 0.84**

结论：**Q4的"双重不确定性"（PV + 价格）实质上塌缩成一个随机驱动**——净负荷偏差决定价格偏差。Q4不需要独立的价格随机过程，净负荷条件预测就够了。

同时存在一个**方差惩罚项**：

$$\mathbb{E}[p_t g_t] = \bar p_t \bar g_t + \beta_t h \operatorname{Var}(\delta_t)$$

实测：$\sum_t \beta_t h \operatorname{Var}(\delta_t) = 3590$ 元/天 = 无电池日均电费的 **7.47%**。这意味着即使风险中性的运营者，降低预测误差也有直接货币价值——CVaR 是尾部精修，不是引入风险厌恶的必要条件。

### 发现5：Q2 是预测问题，绝非"完全信息"（防陷阱）

**关键逻辑**：购电计划在0:00制定，而题目问"若低于负载需向外网紧急购电"——在完全信息下，可令 $g_t = \max(0, L_t - P_t)$，供电永远不缺，紧急购电量**恒为零**。但result2.xlsx 专门设有"紧急购电量"工作表，且表3要求填4个指定日期的紧急购电量。**完全信息下这张表永远空着，题目不会专门设它**。

因此：Q2在0:00时，当日负载和PV数据均不可知，必须从历史序列自建预测。附件2是验证集，不是输入。1月份是冷启动期（result2从2月1日开始），之后每天只能用此前可得数据。

**自建预测精度（已验证）：**

| 方法 | 净负荷槽MAE | 日总量误差std |
|---|---|---|
| 负载：前一天持久法 | 744.2 kW | 24,540 kWh |
| 负载：上周同星期均值 | 176.8 kW | 3,505 kWh |
| PV：近7天均值 | 153.4 kW | 2,986 kWh |
| **组合（上周负载 + 近7天PV）** | **273.4 kW** | **4,640 kWh** |
| 对照：附件3的0:00官方预报 | 197.5 kW | 3,790 kWh |

注意：附件3的官方PV预报对Q2不可用（Q2没有配套预报附件）。Q2必须用自建预测，且负载误差（3505 kWh）与PV误差（2986 kWh）同量级，**两者都必须建模**。

### 发现6：各预报时刻的技能增益（已修正单位）

附件3小时值→能量**必须梯形积分**，否则 6:00/12:00 窗口产生 ±3000 kWh 伪偏差。修正后：

| 目标时段 | 0:00预报MAE | 6:00预报MAE | 12:00预报MAE |
|---|---|---|---|
| 7:00–24:00 | 240.8 kW | 126.0 kW（-47.7%）| — |
| 13:00–24:00 | 186.7 kW | — | 34.2 kW（-81.7%）|
| 19:00–24:00 | ≈0 | ≈0 | ≈0 |

**18:00预报对当日光伏信息量为零**（当日剩余PV均值仅107 kWh）。但18:00行延伸到次日18:00，作为**次日总PV预报**的bias/std = −1320/5284 kWh，是当日唯一的次日日间光伏信息源——其正确用途是**修正次日的终态SOC目标**，而非编辑今天的购电承诺。

### 发现7：Q3的经济学重心与Q2完全不同

Q3有四次重规划机会（0/6/12/18时），每次之后的6小时内才可能发生紧急购电。实测各窗口"未来6小时PV误差"（梯形积分修正后）：

| 窗口 | 6h误差std | P95\|e\| | 可用ESS / P95 |
|---|---|---|---|
| 0:00 | 52 kWh | 121 kWh | 79.4× |
| 6:00 | 882 kWh | 2164 kWh | **4.4×** |
| 12:00 | 703 kWh | 2042 kWh | **4.7×** |
| 18:00 | 154 kWh | 408 kWh | 23.5× |

即使最差窗口（6:00），9600 kWh 可用电池容量仍是 P95 误差的4.4倍，加上计划购电无上限约束，**Q3中称职的策略应当几乎不触发紧急购电**，表3近乎空表。

因此：

$$\boxed{\text{Q2 的成本由 } 5p \text{ 罚金主导（无调整机会，紧急购电是真实风险）}}$$
$$\boxed{\text{Q3 的成本由 } 0.5p \text{ 调整费主导（紧急购电基本可规避）}}$$

这是Q2与Q3最锋利的区别，直接影响建模策略。

### 发现8：一格时间标签错位（工程必查项）

附件数据从 `0:10` 开始（终点标签），result1模板行标签 `0:10-0:20 … 0:00+1-0:10+1`。二者**约定一致**（均为区间终点），145行含表头 = 144个区间。

**必须在论文中明确声明约定**，并对整体平移一格做稳健性检查。若静默错位，每一步SOC递推和全部5个结果文件的数值全错。

---

## 三、四问的真实信息结构

| 维度 | Q1 | Q2 | Q3 | Q4 |
|---|---|---|---|---|
| **计划时PV来源** | 附件1（确定值） | **自建预测**（历史外推） | 附件3官方预报（有误差） | 附件3 + 自建价格预测 |
| **负载来源** | 附件1（确定值） | **自建预测**（上周同星期） | 同Q2（无负载预报附件） | 同Q3 |
| **价格** | 固定（附件1） | 固定（附件1） | 固定（附件1） | 动态（附件4，**无价格预报**，用净负荷条件预测） |
| **终态SOC约束** | $S_{144}=S_0=6000$（硬约束） | 无（用水价软约束替代） | 无 | 无 |
| **主要风险来源** | 无（确定性） | **5p 紧急购电**（无调整机会） | **0.5p 调整费**（紧急购电可规避） | 0.5p + 价格–净负荷耦合 |
| **信息结构** | 完全确定 | 单阶段随机（预测误差） | 多阶段随机（预报滚动更新） | 双重随机（但塌缩为单一驱动） |

---

## 四、统一物理模型

全部采用能量口径，$\Delta t = 1/6$ 小时。功率×$\Delta t$ = kWh。

$$g_t + (P_t - r_t) + d_t + e_t = L_t + c_t$$

$$S_t = S_{t-1} + 0.9\,c_t - \frac{d_t}{0.9}$$

$$1200 \le S_t \le 10800,\quad 0 \le c_t,\, d_t \le \frac{5000}{6},\quad g_t,\, e_t,\, r_t \ge 0$$

其中 $g_t$ 为计划购电，$c_t, d_t$ 为充放电能量（交流侧），$e_t$ 为紧急购电，$r_t$ 为弃光。题目未给上网电价，**不假设售电**；保留 $r_t$ 允许弃光，但单独统计弃光率。

加二元变量 $u^c_t + u^d_t \le 1$ 保证物理可执行性（LP松弛已实测自然满足，但二元约束仍应加入）。

---

## 五、Q1：确定性基准日 + 水价标定

### 建模目标

$$\min \sum_{t=1}^{144} p_t g_t \quad \text{s.t.} \quad S_{144} = S_0 = 6000$$

### 已求得的精确结果

| 指标 | 数值 |
|---|---|
| 全天购电量 | **59,482.70 kWh** |
| 全天购电费 | **35,126.95 元** |
| 无电池基准费 | 48,052.05 元 |
| 电池节省 | **12,925.10 元（26.90%）** |
| SOC摆幅 | 1200–10800（满幅利用） |
| 同时充放电时槽数 | **0**（LP松弛自然满足互斥） |

### Q1 的隐含作用：推导水价 $\lambda^\star$

Q2–Q4 均无终态SOC约束，但每日需要决定"留多少电给明天"。把Q1的终态约束放开做参数扫描：

| 终态SOC (kWh) | 当日最优成本 (元) | 边际水价 (元/kWh) |
|---|---|---|
| 1200 | 32,909.87 | — |
| 3600 | 34,000.27 | +0.4663 |
| **6000** | **35,126.95** | **+0.4706** |
| 8400 | 36,258.10 | +0.4715 |
| 10800 | 37,403.15 | +0.4802 |

因附件1是均值日，$\lambda^\star \approx 0.471$ 元/kWh 就是**稳态储电水价**。Q2–Q4每日模型只需在目标函数加 $-\lambda^\star S_{144}$，跨日耦合问题严格闭合。这个数字**从Q1推导出来**，是四问串联的数学依据，不是启发式猜测。

### Q1论文重点

不只报最优成本，要解释策略形状（用实测的充放时段日志）：深夜低价充电（00:00–01:30，4500 kWh，均价0.426元）→ 早晨高价放电（05:50–09:30，8069 kWh，均价0.940元）→ 午间廉价买电+光伏联合大量充电（09:30–14:30，9962 kWh，均价0.747元）→ 晚高峰大幅放电（18:40–20:40，8531 kWh，均价1.347元）→ 深夜再充（21:50–23:40）。

---

## 六、Q2：日前盲估下的两阶段随机规划

### 信息约束（防"上帝视角"陷阱）

0:00制定计划时，当日实际负载和PV不可知。**必须从附件2历史数据自建预测**，不能把当天的附件2数据直接喂给求解器。全年联合MILP只能作为完美信息下界（benchmark），不能作为Q2的答案。

### 预测层（已验证精度）

- **负载**：前四个同星期日逐10分钟均值（日总量误差std 3505 kWh）
- **光伏**：近7天逐10分钟均值（日总量误差std 2986 kWh）
- 两者近似独立（corr = −0.05），合并不确定性std ≈ 4640 kWh

### 场景生成（必须整日轨迹抽样）

若按时段独立抽样，日内强自相关（lag-1 = 0.966）会被破坏，日总量误差std从3713 kWh压缩到736 kWh（低估5倍），直接吃掉尾部风险。

**正确做法**：从历史误差矩阵中整行重采样（整天的144维误差向量），保留日内相关结构。可按月/季节分层抽样，每组50–200个场景。

### 模型

$$\min_{g^0} \left\{ \sum_t p_t g^0_t + \mathbb{E}_\omega \left[ \min_{c^\omega, d^\omega, e^\omega} \sum_t 5 p_t e^\omega_t \right] + \lambda\, \mathrm{CVaR}_{0.95}(C) \right\}$$

**分层变量**：

- 第一阶段（0:00承诺）：$g^0_t$（144个变量，对外承诺，承诺制结算）
- 第二阶段（追偿变量）：$c^\omega_t, d^\omega_t, e^\omega_t, S^\omega_t$（储能动作和紧急购电，在实际值揭露后决定）

**终态SOC软约束**：目标函数加 $-\lambda^\star S_{144}$（$\lambda^\star = 0.471$ 元/kWh，从Q1推导）。

CVaR扩展：加 $\lambda \cdot \mathrm{CVaR}_{0.95}$ 控制极端紧急购电成本（Rockafellar–Uryasev 线性化，不增加模型类型）。

### 报告指标

- 完美信息下界（全年联合MILP）作EVPI上界
- Q2随机解 vs 确定性解（用均值预测直接LP）的VSS
- 费用-风险-紧急购电三维Pareto比较（LP vs SP vs SP-CVaR）

---

## 七、Q3：预报驱动的多阶段滚动重调度

### 正确的结算公式

最终购电量为 $g_t$（经过0/6/12/18时重规划后），调整前的原计划记为 $g^0_t$。统一结算：

$$\boxed{C_3 = \sum_t \left[ p_t g_t + 0.5\, p_t |g_t - g^0_t| + 5\, p_t e_t \right]}$$

验证（p=1，计划100，调整到140）：$1\times140 + 0.5\times1\times40 = 160$ 元 ✓（与题目规则吻合）

等价展开：减购额外成本 = $0.5p(g^0-g)$；增购额外成本 = $0.5p(g-g^0)$——**在可调整窗口内，偏差惩罚关于方向是对称的0.5p**。

### 是否调整的经济条件（报童临界分位）

| 情形 | 欠买边际成本 | 多买边际成本 | 最优目标分位 $q^*$ |
|---|---|---|---|
| 仍可调整（0.5p对称） | 0.5p | 0.5p | **0.500**（瞄准中位数） |
| Q3末段无调整（18:00后） | 4.0p | ≈0.7p | **0.852** |
| Q2全天无调整 | 4.0p | ≈0.7p | **0.852** |

推论：**Q3的0:00计划中，6:00/12:00之后的时槽应当瞄准净需求的中位数**，而非Q2那样的高分位——因为调整机会已保证了对称的低成本纠偏能力。全天统一右偏会系统性多买，每单位白扔0.5p。只有18:00后的时槽（彼时无调整机会）才回到0.85分位。

### 调整策略

- **0:00**：用附件3的0:00预报 + 自建负载预测，按上述分层分位制定初始计划
- **6:00**：以实际SOC为起点，对余下108槽重求解。已执行时段永不回写
- **12:00**：同上，对余下72槽重求解（含最大PV信息增益，−81.7% MAE）
- **18:00**：当日光伏已近零，**不修改当日购电承诺**；但将18:00预报的次日PV信息转化为次日终态SOC目标（即更新明日的 $\lambda^\star$）

### 预报价值定量分析（Q3的论文核心）

对全年每天计算四种策略的实际费用并统计其差值：

1. 仅0:00预报，不调整
2. 0:00 + 6:00
3. 0:00 + 6:00 + 12:00
4. 0:00 + 6:00 + 12:00 + 18:00（18:00仅更新次日SOC目标）

预期结论：6:00贡献最大（接近全部光照时段的不确定性），12:00次之，18:00对当日贡献近零但对次日跨日有贡献。按春分/夏至/秋分/冬至分组展示季节差异。

---

## 八、Q4：价格–净负荷耦合下的条件场景规划

### 价格建模（不独立处理价格不确定性）

利用已验证的线性耦合关系（R²=0.84）：

$$\hat p_{d,t} = \bar p_t + \beta_t \left(\hat L_{d,t} - \hat P_{d,t} - \overline{(L-P)_t}\right)$$

其中 $\bar p_t$ = 附件1电价（均值日），$\hat L, \hat P$ = Q2/Q3的自建预测，$\beta_t$ 用前一个月拟合（1月冷启动）。

6:00/12:00时，已观测到的实际净负荷可再更新价格预测。

### 场景生成

不确定性向量 $\xi_t = (\varepsilon^L_t, \varepsilon^{PV}_t)$（负载误差 + PV误差），**价格误差通过 $\beta_t$ 从净负荷误差中导出，不独立抽样**。这自动保留了价格–净负荷 corr=0.92 的耦合，避免独立处理时低估联合尾部风险。

整日轨迹联合抽样（与Q2相同方案）。

### 模型主线

沿用Q3的SMPC框架，替换：固定电价 $p_t$ → 条件预测电价 $\hat p_{d,t}$。

扩展（DRO）：Wasserstein-1球 + 分段线性成本 → **可归约为LP**（Mohajerin Esfahani & Kuhn 2018），不需要SDP/SOCP，cvxpy + HiGHS直接可解。Wasserstein球半径按月估计（夏季球大，冬季小）。

### 方差惩罚提供建模动机

$$\sum_t \beta_t h \operatorname{Var}(\delta_t) = 3590 \text{ 元/天} \approx 7.47\%\ \text{电费}$$

这是Q4相比Q2/Q3多出的**系统性成本项**：Q2/Q3用均值电价会低估真实电费 ≈2167 元/天（$\sum_t \operatorname{cov}(p_t, g_t)$）。这也是Q4比Q3在经济意义上更重要的数学证明，不只是"加了个随机电价"。

---

## 九、方法总体推荐

| 问题 | 主方法 | 辅助/验证 | 数学亮点 |
|---|---|---|---|
| Q1 | 确定性MILP（含互斥）| LP松弛验证（已实测重合）；DP（可选） | Q1水价 $\lambda^\star=0.471$；储能节省26.9% |
| Q2 | 两阶段SP（整日轨迹场景）+ CVaR | 完美信息MILP作EVPI下界 | 5p主导；独立抽样低估5倍的陷阱 |
| Q3 | 多阶段确定性MPC + 阈值触发调整 | 随机MPC作高档备选 | 0.5p主导；分层分位（可调段0.5，末段0.85）；18:00用于次日 |
| Q4 | 净负荷条件价格预测 + Q3 SMPC框架 | DRO（W1球→LP）作扩展 | 双重不确定塌缩为单驱动；方差惩罚7.47% |

### 论文主线（串联，非孤立四问）

$$\underbrace{Q1}_{\text{均值日水价}} \;\rightarrow\; \underbrace{Q2}_{\text{预测误差下的日前承诺}} \;\rightarrow\; \underbrace{Q3}_{\text{预报更新驱动的日内重调度}} \;\rightarrow\; \underbrace{Q4}_{\text{价格–净负荷耦合的分布鲁棒调度}}$$

---

## 十、已确认的错误（防重蹈）

1. **"上帝视角"陷阱（最致命）**：Q2不是完全信息问题。把附件2当0:00已知输入，紧急购电永远为零，完全架空5倍惩罚机制，表3无数据可填。Q2必须自建预测，附件2只用于事后验证。

2. **Q2"全年联合MILP"不是答案**：只能作完美信息下界（benchmark）。

3. **时段独立抽样破坏场景质量**：日内自相关lag-1=0.966，独立抽样使日总量风险低估5倍。必须整日轨迹重采样。

4. **Q3调整费对称性**：可调整窗口内，增减购电的额外成本都是0.5p（对称）。全天统一右偏是Q3特有的错误，Q2才应全天右偏到0.85分位。

5. **附件3小时值→能量需梯形积分**：朴素求和在6:00/12:00窗口产生±3000 kWh伪偏差，必须在论文中说明插值方案。

6. **互斥约束说法**：不宜写"电价比>1.23故LP必然套利"——已实测LP松弛自然满足互斥。正确说法是加二元变量为物理可执行性背书。

7. **DRO不需要SDP**：Wasserstein-1球+分段线性成本可归约为LP，不需要MOSEK，cvxpy+HiGHS可解。

8. **18:00预报对当日无用，对次日有用**：不是"18:00预报无意义"，而是用途在跨日SOC定位，不在当日购电调整。

9. **负载是被忽视的主要不确定性**：负载占合并不确定性方差的58%，Q2/Q3/Q4都没有负载预报附件，自建预测是必要的而不是可选的。

---

## 十一、必须写入论文的工程说明

1. **时间标签约定**：统一采用区间终点，附件行`0:10`=区间(0:00,0:10]，result行`0:10-0:20`=区间(0:10,0:20]——两者均为终点标签，但附件比result早一个槽的标签。对整体平移一格做稳健性检查并报告。
2. **小时预报→10分钟能量**：采用梯形积分（相邻整点值取均值后乘1小时），不能直接将小时均值乘1/6当作10分钟值。报告修正前后的对比（±3000 kWh差异）。
3. **场景生成方案**：整日误差轨迹（144维）整行重采样，不按时段独立抽样。报告独立采样与轨迹采样的日总量风险对比。
4. **水价参数**：$\lambda^\star=0.471$ 元/kWh，来源于Q1终态SOC参数扫描，非启发式猜测。

---

*本文档基于题目精读、全部附件数据实测验证、三版方案（我方初版、队友版、新版本）独立对比分析后的综合结论。全部数值均已通过 Python 脚本独立复现。*
